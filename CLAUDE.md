# haboku

ローカルの Markdown ファイルをそのまま扱うメモアプリ。1200 件規模の vault を即座に開き、検索して書ける。

## Done の定義

<!-- 検証可能な形（実行できるコマンド・観測できる状態・数字）で書く。数字やコマンドに落ちないものは完了判定に使わない -->

実データ vault（約 1200 件）で以下がすべて満たせること。

- 起動時の vault 読み込みが **50ms 以内**（暖機後。gpui 版の実測は 47.6ms）
- ノート一覧をスクロールしても引っかからず、打鍵時の `view()` 構築が **1ms 未満**
- `Cmd+P` でノートを絞り込み、`Enter` で開ける
- 編集して手を止めると **1 秒以内**に自動保存され、打ち続けても **2.5 秒以内**に必ず保存される
- **何も編集せずノートを切り替えたときはディスクに書かない**（mtime が動かない）
- 保存に失敗したらノート切替が中断され、編集内容がエディタに残る
- 日本語の本文とコードブロックが**1 文字も欠けずに**表示される（漢字が消えないこと）
- `cargo test` が通り、`cargo clippy --all-targets` が警告なし

## スタック

Rust 2024 edition / [iced](https://github.com/iced-rs/iced) 0.14（features: `highlighter`, `advanced`, `tokio`）。
シンタックスハイライトは syntect（`iced_highlighter` 経由）。

選定理由と却下した代替案は `docs/decisions/` を参照。非標準な技術を足すときは innovation token を1枚払う意識で、ADR に理由を残す。**ADR と `docs/spec.md` は手元にだけ置く**（git 管理外。`.gitignore` 参照）。

## 由来

gpui + gpui-component で書いた前身がある（凍結）。
移植可否は移植検証のリポジトリで実測済みで、**踏んだ罠と実測値はそこの
モジュールドキュメントとコミットメッセージに全部ある**。iced で詰まったらまずそこを読むこと。

前身から**無改造で持ち込めるもの**: `src/vault.rs`（vault 読み込み・frontmatter パース・保存）、
`src/fuzzy.rs`（ファジー検索）。どちらも std のみに依存し UI 層への参照がゼロ。

## 進め方の型

- **Plan-first**: 非自明な機能は Research → Plan → 実装の順。プランに合意するまで実装しない。不慣れな領域では着手前に blind spot pass（この領域で見落としがちな点・自分が問うべきだった問いを先に列挙させる）を行う
- **BSSN / YAGNI**: 今の要求だけを解く。投機的な抽象化・汎用化・未使用の拡張点を作らない。取り除いても今の役目を果たせるものは入れない
- **小さく回す**: 変更は小さく・理由は1つ。依頼外の drive-by リファクタをしない。前の変更が閉じる前に次を積まない
- **証拠で完了判定**: 「動きました」ではなく、テスト結果・実行ログ・スクリーンショットで示す
- **理解してから ship**: 自分が説明できない diff はコミットしない

## この題材で特に効く規律

- **GUI のバグは、状態と表示のどちらが壊れているかを数字で先に分ける。** 前身では「元ファイル 2192 文字 = 画面の文字数 2192」の照合で、疑う層をレンダリングだけに絞り込めた
- **「どこで壊れたか」より「何が壊れたか」を先に見る。** 日本語が欠ける件で折り返し位置に気を取られたが、消えていたのは全部漢字で、真因はフォントのフォールバック失敗だった
- **安全装置は、失敗したときに何が起きるかまで設計する。** 読み込み専用モードは借り物データを守るつもりで「打てるのに保存されない＝書いた本文が消える」経路を作っていた（`docs/decisions/` 参照）

## 記録の規約

**追跡するのは `docs/implementation-notes.md` だけ。** ADR・spec・レビュー・research は
手元に置き、git 管理から外してある（`.gitignore`）。書く場所は変わらないが、**公開物には出ない**。

- 設計判断（採用・不採用と理由）→ `docs/decisions/` に ADR として残す。チャットに置き去りにしない
- 実装中の計画からの逸脱・現場判断 → `docs/implementation-notes.md` の Deviations に追記
- 同じ手順を Claude に2回説明したら → `.claude/skills/` にプロジェクトスキルとして切り出す
- 仕様の現状は `docs/spec.md` が source of truth。経緯は ADR へ分離する

## セキュリティ最小則

- 秘密情報は `.env` に置き、コードに直書きしない。`.env` は必ず `.gitignore` に含める
- 「非公開データへのアクセス・信頼できない外部コンテンツ・外部送信」の3つが同時に成立する経路を作らない（Lethal Trifecta）

<!-- このファイルは短く・安定に保つこと。日付・カウンタ・UUID などの可変値を書かない（プロンプトキャッシュ保護）。長くなってきたら詳細を docs/ や .claude/skills/ に逃がす -->
